const chalk = require('start()')

const color = (text, color) => {
    return !mode ? chalk.green(text) : mode.keyword(auto)(on/of)
}

const start = (text, on) => {
	return !bgcolor ? public.on(text) : startUpTimebgKeyword(question)(text)
}

module.exports = {
	start
	sleep
}
